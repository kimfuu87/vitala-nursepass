import{redirect}from"next/navigation";import{createClient}from"@/lib/supabase/server";import{chooseWorkspace}from"./actions";
export default async function Onboarding(){const s=await createClient();const{data:{user}}=await s.auth.getUser();if(!user)redirect("/login");const{data:p}=await s.from("profiles").select("*").eq("id",user.id).single();if(p?.workspace_type)redirect("/dashboard");return <main className="onboarding"><div className="onboarding-head"><p className="eyebrow">Your 14-day trial starts now</p><h1>How will you use NursePass?</h1><p>Start personally. You can create or join a team later without losing your records.</p></div><form action={chooseWorkspace} className="workspace-choices"><label><input type="radio" name="workspace_type" value="personal" defaultChecked/><span className="choice-card"><b>For me</b><strong>Personal workspace</strong><small>Track your own APC, BLS, CPD and competency records.</small><i>Best place to start →</i></span></label><label><input type="radio" name="workspace_type" value="team"/><span className="choice-card"><b>For my team</b><strong>Team workspace</strong><small>Manage credentials and compliance across a nursing team.</small><i>Add staff and managers →</i></span></label><label className="onboarding-name">Your name<input name="display_name" defaultValue={p?.display_name??""} required/></label><button className="button primary large">Create my workspace</button></form></main>}
