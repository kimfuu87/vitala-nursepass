import Link from "next/link";
import { signIn, signUp } from "@/app/auth/actions";
export default async function Login({searchParams}:{searchParams:Promise<{error?:string;message?:string}>}) {
  const p=await searchParams;
  return <main className="auth-shell"><section className="auth-story"><Link className="brand light-brand" href="/"><span>V</span> Vitala NursePass</Link><div><p className="eyebrow">Secure nursing operations</p><h1>Your team’s credentials, beautifully organized.</h1><p>Expiry tracking, CPD, competency and audit history in one private workspace.</p></div><blockquote>“Know what is valid, what needs attention, and what comes next.”</blockquote></section><section className="auth-form"><div><Link className="back" href="/">← Back to home</Link><p className="eyebrow">Welcome</p><h2>Sign in to NursePass</h2><p>Use your work email to continue.</p>{p.error&&<div className="error-banner">{p.error}</div>}{p.message&&<div className="success-banner">{p.message}</div>}<form className="login-form"><label>Email address<input name="email" type="email" placeholder="you@hospital.my" required/></label><label>Password<input name="password" type="password" minLength={8} placeholder="At least 8 characters" required/></label><button formAction={signIn} className="button primary large">Sign in securely</button><div className="auth-divider"><span>New to NursePass?</span></div><button formAction={signUp} className="button large">Create an account</button></form><p className="fine-print">Your documents stay protected with owner-scoped access.</p></div></section></main>;
}
