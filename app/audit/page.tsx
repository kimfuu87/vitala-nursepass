import Link from "next/link"; import { redirect } from "next/navigation"; import { createClient } from "@/lib/supabase/server";
export default async function Audit(){const s=await createClient();const{data:{user}}=await s.auth.getUser();if(!user)redirect("/login");const{data}=await s.from("audit_logs").select("*").order("created_at",{ascending:false}).limit(100);return <main className="shell"><Link className="back" href="/">← Dashboard</Link><section className="panel"><h1>Audit trail</h1><div className="slots">{(data??[]).map(log=><article className="staff-card" key={log.id}><div className="staff-main"><h3>{log.action} · {log.target_table}</h3><p>{log.actor_email??"Demo/system"} · {new Date(log.created_at).toLocaleString("en-MY")}</p></div></article>)}</div></section></main>}
